# Build output of scripts/build-examples.mjs. Committed so tests and the typed
# manifest import work without a build step, and regenerated by `predeploy` so a
# deploy can never ship a stale manifest. Marked generated to keep diffs quiet.
src/examples/manifest.generated.ts linguist-generated=true -diff

# Build output of scripts/build-frontend.mjs. Committed so wrangler deploy works
# without a pre-step; regenerated by `predeploy`. Marked generated to keep diffs quiet.
public/app.js linguist-generated=true -diff
